name: Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - uses: arduino/setup-protoc@v1
      - run: bash proto.sh
      - run: npm install -g @angular/cli
      - run: cd web && npm install && ng build --output-path ../cmd/autograder-web
      - run: go build cmd/autograder_server.go
      